@{
    ViewData["Title"] = "Nivel 1";
}

<div class="container level-shell">
    <div class="row justify-content-center">
        <div class="col-lg-9">
            <h1 class="level-title scanline-text">🔐 Nivel 1</h1>

            <div class="level-banner crt-frame">
                <img src="/imagenes/comodin/Odd_Todd.webp" alt="">
                <div>
                    <span class="escape-badge pixel-label">Consigna de acceso</span>
                    <h2 class="mt-3 mb-2">Patrón oculto</h2>
                    <p class="text-muted">Un símbolo debería ocupar el lugar vacío. Descubrí la secuencia y elegí la opción correcta.</p>
                </div>
            </div>

            <div class="card mb-4 level-card crt-frame">
                <div class="card-header">
                    <h5 class="mb-0">📝 Desafío</h5>
                </div>
                <div class="card-body">
                    <div class="level-prompt scanline-text consigna-html">@Html.Raw(ViewBag.Nivel.Desafio)</div>
                </div>
            </div>

            <form action="@Url.Action("VerificarRespuesta", "Home")" method="post">
                <input type="hidden" name="id" value="@ViewBag.Nivel.Id" />
                <div class="mb-3">
                    <input type="hidden" name="pistasUsadas" id="pistasUsadas" value="0"/>
                    <label for="respuesta" class="form-label">Tu Respuesta:</label>
                    <input type="text" class="form-control form-control-lg" id="respuesta" name="respuesta" placeholder="Ingresa la respuesta..." required />
                </div>
                <button type="submit" class="btn btn-success btn-lg w-100">Enviar Respuesta</button>
            </form>

            <hr />

            <div class="mt-4">
                <small class="text-muted d-block mt-2">Pistas disponibles: @ViewBag.Pistas</small>
                <button class="btn btn-outline-light w-100" type="button" id = "botonPista" onclick="mostrarPista(@ViewBag.Pistas)">
                    💡 Mostrar pista
                </button>
                <div class="collapse mt-3">
                    <div class="card level-card level-hint-shell crt-frame">
                        <div class="card-header">
                            <h6 class="mb-0">💡 Pista</h6>
                        </div>
                        <div class="card-body">
                            <p id = "pista"></p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="mt-4">
                <a href="@Url.Action("CerrarSesion", "Home")" class="btn btn-outline-light">Volver al Inicio</a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        async function mostrarPista(pistasDisponibles) {
        var pista = document.getElementById('pista');
        var pistasUsadas = document.getElementById('pistasUsadas');
            if (pistasDisponibles > 0) {
                pista.innerText = "@ViewBag.Nivel.Pista";
                pistasUsadas.value = parseInt(pistasUsadas.value) + 1;
            }
            else {
                document.getElementById('botonPista').disabled = true;
                pista.innerText = "No hay más pistas disponibles.";
            }
        }

        document.querySelector('form').addEventListener('submit', function(e) {
            var respuesta = document.getElementById('respuesta').value.trim();
            if (!respuesta) {
                alert('Por favor ingresa una respuesta');
                e.preventDefault();
            }
        });
    </script>
}
